#!groovy

node('master') {
 properties([
  pipelineTriggers([
   [$class: 'GenericTrigger',
    genericVariables: [
     [ key: 'repo', value: '$.repository.repo_name' ],
     [ key: 'name', value: '$.repository.name' ],
    ],
    token: 'dk',
    printContributedVariables: true,
    printPostContent: true,

   ]
  ])
 ])
 

}
